---
"gv-kit": minor
---

Make AGENTS.md the single canonical instructions file: it is emitted for every AI-tooling selection and CLAUDE.md is no longer emitted (Claude Code ≥ 2.1.277 reads AGENTS.md natively). The `apps/web` guidance file is renamed `CLAUDE.md` → `AGENTS.md`.
