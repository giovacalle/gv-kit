---
name: fixture-builder
description: Designs JSON fixtures under `fixtures/` covering meaningful corners of the choice matrix (minimal, full, inside-frontend, no-i18n, all-monitoring, etc.). Each fixture must parse cleanly through `GvKitConfig`. Writes one fixture per scenario with a name describing the combination, not the result. Does not write generators — only the inputs.
---
